<section class="relative min-h-screen flex flex-col justify-center items-center text-center px-4 md:px-10 overflow-hidden">

  <!-- ✅ Animated Background Gradient -->
  <div class="absolute inset-0 -z-10 animate-gradient bg-gradient-to-r from-blue-600 via-purple-500 to-pink-500 opacity-30 blur-2xl"></div>

  <!-- ✅ Profile Photo -->
  <img
    src="profile.jpg"
    alt="Pawan Sharma"
    class="w-40 h-40 sm:w-48 sm:h-48 rounded-full shadow-lg border-4 border-white dark:border-gray-800 mb-6 transform transition duration-1000 opacity-0 translate-y-4 hover:ring-4 hover:ring-blue-400 hover:scale-105 animate-fade-in"
    loading="lazy"
  />

  <!-- Heading -->
  <h1 class="text-3xl sm:text-4xl md:text-6xl font-extrabold text-gray-900 dark:text-white mb-4 leading-tight">
    Hi,</br> I'm <span class="text-blue-600 dark:text-blue-400">Pawan Kumar Sharma</span>
  </h1>

  <!-- ✅ Looping Typewriter -->
  <p id="typewriter" class="text-lg sm:text-xl text-blue-600 dark:text-blue-400 font-semibold mb-4 h-[1.5em]">
    <!-- Text will be injected by JS -->
  </p>

  <!-- Description -->
  <p class="text-sm sm:text-md md:text-lg text-gray-600 dark:text-gray-400 max-w-2xl mb-8">
    I build fast, clean, and modern web & mobile apps using the latest technologies.
  </p>

  <div class="flex flex-wrap justify-center gap-4">
    <a href="/projects" class="bg-blue-600 text-white px-6 py-3 rounded-lg font-medium hover:bg-blue-700 transition duration-300">
      View Projects
    </a>
    <a href="/contact" class="bg-gray-200 dark:bg-gray-800 text-gray-900 dark:text-white px-6 py-3 rounded-lg font-medium hover:bg-gray-300 dark:hover:bg-gray-700 transition duration-300">
      Contact Me
    </a>
  </div>
</section>

<style is:global>
  @keyframes gradientShift {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }
  @keyframes fadeInUp {
    0% { opacity: 0; transform: translateY(1rem); }
    100% { opacity: 1; transform: translateY(0); }
  }
  .animate-gradient {
    background-size: 200% 200%;
    animation: gradientShift 10s ease infinite;
  }
  .animate-fade-in {
    animation: fadeInUp 1.2s ease-out forwards;
  }
</style>

<script>
  const roles = [
    "Full-stack Developer",
    "Open Source Contributor",
    "UI/UX Enthusiast",
    "Problem Solver"
  ];

  let roleIndex = 0;
  let charIndex = 0;
  let currentText = "";
  let isDeleting = false;
  const speed = 100; // Typing speed
  const pause = 1500; // Pause before deleting

  const typewriterElement = document.getElementById("typewriter");

  function typeLoop() {
    const role = roles[roleIndex];
    
    if (isDeleting) {
      currentText = role.substring(0, charIndex--);
    } else {
      currentText = role.substring(0, charIndex++);
    }

    typewriterElement.textContent = currentText;

    if (!isDeleting && charIndex === role.length) {
      setTimeout(() => isDeleting = true, pause);
    } else if (isDeleting && charIndex === 0) {
      isDeleting = false;
      roleIndex = (roleIndex + 1) % roles.length;
    }

    const delay = isDeleting ? speed / 2 : speed;
    setTimeout(typeLoop, delay);
  }

  typeLoop();
</script>
